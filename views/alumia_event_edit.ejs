<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Event Details</title>
    <link rel="stylesheet" href="/assets/edit_event.css">
    <style>
        /* General Styles */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f9f9f9;
        }

        header {
            background-color: #333;
            color: white;
            padding: 20px;
            text-align: center;
        }

        footer {
            background-color: #333;
            color: white;
            text-align: center;
            padding: 10px 0;
            width: 100%;
        }

        main {
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 80vh;
        }

        .event-details {
            background-color: white;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            overflow: hidden;
            display: flex;
            flex-wrap: wrap;
            max-width: 900px;
        }

        .event-image img {
            width: 100%;
            height: auto;
            border-bottom: 1px solid #ddd;
        }

        .event-info {
            padding: 20px;
            flex: 1 1 50%;
        }

        .event-info label {
            font-weight: bold;
            color: #333;
            margin-top: 10px;
            display: block;
        }

        .event-info input,
        .event-info textarea {
            width: 100%;
            padding: 8px;
            margin: 5px 0 15px 0;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 16px;
        }

        .event-info input[type="submit"] {
            background-color: #007BFF;
            color: white;
            border: none;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .event-info input[type="submit"]:hover {
            background-color: #0056b3;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .event-details {
                flex-direction: column;
            }

            .event-image img {
                border-bottom: none;
            }
        }

        @media (max-width: 480px) {
            .event-title {
                font-size: 20px;
            }

            .event-info input,
            .event-info textarea {
                font-size: 14px;
            }

            .event-info input[type="submit"] {
                width: 100%;
                font-size: 18px;
            }
        }

    </style>
</head>
<body>
    <header>
        <h1>Edit Event Details</h1>
    </header>

    <main>
        <section class="event-details">
            <div class="event-image">
                <img src="<%= data.image %>" alt="Event Image">
            </div>
            <div class="event-info">
                <form action="/Admin/event/<%= data.title %>+apply_form/update-event" method="POST">
                    <input type="text" style="display: none;" id="image" name="image" value="<%= data.image %>">
                    <input type="text" style="display: none;" id="title" name="title" value="<%= data.title %>">

                    <h1><%= data.title %></h1>
                    <label for="description">Description:</label>
                    <textarea id="description" name="description" rows="4"><%= data.description %></textarea>

                    <label for="date">Date:</label>
                    <input type="text" id="date" name="date" value="<%= data.date%>">

                    <label for="time">Time:</label>
                    <input type="text" id="time" name="time" value="<%= data.time %>">

                    <label for="location">Location:</label>
                    <input type="text" id="location" name="location" value="<%= data.location %>">

                    <label for="organizer">Organizer:</label>
                    <input type="text" id="organizer" name="organizer" value="<%= data.organizer %>">

                    <label for="eventType">Type:</label>
                    <input type="text" id="eventType" name="eventType" value="<%= data.EventType %>">

                    <label for="contact">Contact Email:</label>
                    <input type="email" id="contact" name="contact" value="<%= data.ContactEmail %>">

                    <label for="website">Website:</label>
                    <input type="url" id="website" name="website" value="<%= data.Website %>">

                    <label for="status">Status:</label>
                    <input type="text" id="status" name="status" value="<%= data.Status %>">

                    <input type="submit" value="Save Changes">
                </form>
            </div>
        </section>
    </main>

    <footer>
        <p>&copy; 2024 AlumniConnect</p>
    </footer>
</body>
</html>
